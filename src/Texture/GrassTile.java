package Texture;

import Graphics.AssetManager;

import java.awt.image.BufferedImage;

public class GrassTile extends Tile {
    public GrassTile(int id) {
        super(AssetManager.grass, id);
    }
}
